
> PULL HR & LibUtil
> run UPDATE-DB.sql

> TABLE Payrolls
	- Id (guid)
	- Timestamp (datetime)
	- Employee_UserAccounts_Id
	- Amount (decimal(10,0))

> TABLE PayrollItems 
	- Id (guid)
	- Payrolls_Id (guid)
	- RefId (guid) --> bisa Attendance_Id, Payrule_Id?
	- Description (nvarchar(MAX))
	- Amount (decimal(10,0))
	- Notes (nvarchar(MAX))

> TABLE Attendance ADD PayrollItems_Id
	- if not null, locked (no further modification is allowed). 

> Payroll_Main_Form
- copy dari iSpeak project: SaleInvoices_Main_Form.cs
	* can pay multiple payrolls at once
- filter: 
	* Start Date, End Date
	* UserAccounts_Id

