
> PULL HR & LibUtil
> run UPDATE-DB.sql

> TABLE Payments
	- Id (guid, not null) 
	- No (nvarchar(MAX), not null)
	- Timestamp (datetime, not null)
	- Source_BankAccounts_Id (guid)
	- Target_BankAccounts_Id (guid)
	- Amount (decimal(10), not null)
	- ConfirmationNumber (nvarchar(MAX))
	- Notes (nvarchar(MAX))
	- Approved (bit, default 0, not null)
	- Rejected (bit, default 0, not null)

> TABLE PaymentItems 
	- Id (guid, not null)
	- Payments_Id (guid, not null)
	- Transaction_RefId (guid, not null) --> bisa Payrolls_Id, POs_Id, etc
	- Amount (decimal(10,0), not null)
	- Notes (nvarchar(MAX))

> TABLE BankAccounts ADD Internal (bit,0,not null)
	- modify MasterData_v1_BankAccounts: see MasterData_v1_UserAccountRoles (similar to special field)

> Payments_Add_Form
	- similar to iSpeak Payments_Add_Form, add/remove controls as needed.
	- show dropdownlist of all BankAccounts where internal=true (Source_BankAccounts_Id)
	- show dropdownlist of all BankAccounts owned by the employee (Target_BankAccounts_Id)

> Payments_Main_Form
	- copy dari iSpeak project: Payments_Main_Form.cs
		* can pay multiple payrolls at once
	- add datagridview to show PaymentItems data
	- filter: 
		* Start Date, End Date
		* UserAccounts_Id
		* Payroll No
		* Source Bank Account
		* Target Bank Account
	- 

